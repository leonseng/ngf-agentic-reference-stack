apiVersion: v1
kind: Service
metadata:
  name: agent-zero
  labels:
    app: agent-zero
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: agent-zero
